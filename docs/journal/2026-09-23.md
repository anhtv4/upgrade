# 2026-09-23 - Setup du an va he thong theo doi

**Phase:** 1 | **Thoi luong:** ~? (dien sau)

## Muc tieu buoi
Chuan bi repo va cach lam viec truoc khi bat dau Phase 1.

## Da lam
- Khoi tao git, commit skeleton vao nhanh `skeleton-reference`, them `.gitignore` (bo `.idea/`, `target/`).
- Tao nhanh `main` tu commit skeleton roi xoa code Java -> nguoi lam tu viet lai tu dau. Push ca 2 nhanh len GitHub.
- Dung he thong theo doi tien do trong repo: `docs/progress.md`, `docs/journal/`, `docs/competencies.md`, skill `/bat-dau` `/ket-thuc` `/goi-y` `/kiem-tra`, `.claude/settings.json` (hoi truoc khi Claude sua `src/`).
- `CLAUDE.md` viet lai thanh tu du, uu tien hon quy tac global.

## Kho khan / bac hint da dung
- `git push` that bai vi nhanh chua co commit nao (moi `git add`, chua `git commit`).

## Rut ra
- Push chi day commit - file staged chua commit thi khong co gi de day. Loi dien hinh: `src refspec <branch> does not match any`.
- Nen co `.gitignore` truoc commit dau tien: commit goc la nen cua moi nhanh sau nay.
- Chon nhanh `main` re tu skeleton (thay vi `--orphan`) de 2 nhanh chung lich su -> `git diff` so sanh tu nhien.

## Cau hoi con treo
- (chua co)

## Buoc tiep theo
Tai lieu bounded context - bat dau voi cau hoi "Provider thuoc context nao?".
